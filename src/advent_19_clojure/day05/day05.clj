(ns eatobin.day05
  "FIXME: my new org.corfield.new/scratch project.")

;; (defn exec
;;   "Invoke me with clojure -X eatobin.day05/exec"
;;   [opts]
;;   (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m eatobin.day05"
  [& args]
  (println "-main with" args))
